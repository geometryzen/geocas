/**
 * Returns sign change due to putting the blade blades represented
 * by <code>a<code> and <code>b</code> into canonical order.
 */
export default function canonicalReorderingSign(a: number, b: number): number;
